# Зависимости для Vercel: их ставит сборщик python-функции.
# Локально проект живёт на uv (pyproject.toml + uv.lock) — здесь только то,
# что нужно веб-демо: jupyter/matplotlib из pyproject функции не требуются.
openai>=2.30.0
pydantic>=2.12.5
python-dotenv>=1.2.2
requests>=2.33.0
