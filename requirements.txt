# Зависимости рантайма для Vercel. Сборщик python-функции читает
# pyproject.toml, если он есть, поэтому список обязан совпадать с
# [project].dependencies — иначе в бандл уедет что-то лишнее.
openai>=2.30.0
pydantic>=2.12.5
python-dotenv>=1.2.2
requests>=2.33.0
